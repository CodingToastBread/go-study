# HOW TO STUDY — 이 저장소와 pricewatch 를 어떻게 쓰는가

2026년 9월, Go 로 첫 제품을 만들기로 하면서 정한 공부 방법입니다.
방법이 흔들릴 때 다시 읽는 문서입니다. 규칙은 적을수록 지켜지므로, 여기 있는 것 이상으로 늘리지 않습니다.

---

## 0. 전제

- 문법 복습은 따로 하지 않는다. 기억 안 나는 항목은 그때 찾아본다.
- 다른 언어와 비교하지 않는다. Go 는 Go 의 이유로 이해한다.
- 목표는 "Go 를 아는 것"이 아니라 **"12월 31일에 URL 이 있고, 누군가 쓰고 있고, 코드는 전부 내가 쓴 Go 제품이 있는 것"** 이다.

---

## 1. 큰 그림: 탑다운

빈 파일에서 시작하지 않는다. 완성된 제품(pricewatch)을 먼저 받아서 실행해보고,
매주 패키지 하나를 잡아 **읽고 → 바꾸고 → 지우고 다시 쓴다.**
10주차에 저장소 전체를 비우고 일주일 안에 기억으로 다시 쓴다. 배포되는 것은 그 버전이다.

이 방식이 실패하는 유일한 패턴은 **읽기만 하고 끝나는 것**이다.
잘 짜인 코드는 읽으면 다 이해되는 것 같은 착각을 준다. 그래서 아래 세 단계가 있다.

---

## 2. 매주 반복하는 세 단계

### ① 읽기 — 왜 이렇게 짰는지 묻는다

- 패키지를 위에서 아래로 읽는다. `main.go` 에서 시작하면 전부 따라갈 수 있다.
- IDE 의 *Go to Definition* 과 *Find Implementations* 를 계속 누른다. 인터페이스 구현이 암묵적이라 후자가 특히 중요하다.
- **이해 안 되는 줄을 그냥 넘기지 않는다.** 그 줄이 그 주의 질문이다. 모아뒀다가 Claude 에게 던진다. 다섯 개 이상 나오는 게 정상이다.
- 좋은 질문의 모양: "이 인터페이스는 왜 여기(쓰는 쪽)에 있어?", "이 goroutine 은 누가 끝내?", "이 에러를 여기서 감싸는 이유가 뭐야?", "더 Go 다운 방법이 있어?"

### ② 바꾸기 — 기능 하나를 추가한다

- 그 패키지에 실제 기능을 하나 붙인다 (텔레그램 알림, 임계값 옵션, 새 컬럼 등).
- 원본의 스타일을 따라 쓴다. 따라 쓰다 보면 관용구가 손에 붙는다.
- 기능 하나를 추가할 때 **손이 가는 파일이 몇 개인지 센다.** 그 수가 설계를 말해준다.

### ③ 지우고 다시 짜기 — 기억으로

- 패키지 폴더를 지운다. **테스트 파일과 마이그레이션은 남긴다.** 테스트가 명세다.
- 빈 파일에서 기억으로 다시 쓴다. 테스트가 초록색이 되면 끝.
- 막히면 원본을 **"확인하고 닫기"** 로만 쓴다. 열어두고 베끼면 이 단계는 의미가 없다.
- 끝나면 원본과 diff 를 놓고 Claude 에게 리뷰를 받는다. Go 답지 않은 곳 / 원본보다 나아진 곳을 각각 찾는다.
- 안 되면 다시 지우고 다시 쓴다. **이게 되는 패키지가 늘어나는 것이 진행률이다.**

---

## 3. 작은 단위의 같은 방법: 읽고, 닫고, 기억으로 치고, 비교하기

코드를 따라치는 것은 바보 같은 일이 아니다. 방식의 문제다.

- **효과가 작은 방식**: 화면을 보면서 한 글자씩 옮기기. 눈은 원본에, 손은 자동으로. 머리가 개입하지 않는다.
- **효과가 큰 방식**: 블록 하나를 읽고 이해했다 싶으면 **파일을 닫고** 빈 파일에 그 블록을 다시 쓴다. 안 되면 열어서 확인하고, 다시 닫고, 다시 친다.

이것은 2절의 ③ 을 함수 하나 크기로 줄인 것이다. `tour/` 와 `basics/` 를 통째로 따라치지 않는다.
손에 붙이고 싶은 블록만 골라서 이 방식으로 친다. 예: `01-syntax` 의 functions·closures,
`03-types` 의 errorsWrapping, `04-concurrency` 의 channels·select·workerPool·contexts.

---

## 4. 규칙 네 개

1. **튜토리얼로 돌아가지 않는다.** 문법이 기억 안 나면 Go by Example 에서 그 항목 하나만 보고 바로 코드로 돌아온다. "복습 주간"은 없다.
2. **매주 실행 가능한 상태로 끝낸다.** 주말에 `go build ./... && go test -race ./...` 가 통과하고 커밋이 있어야 그 주는 끝난 것이다. 기능이 반쪽이어도 된다.
3. **30분 이상 막히면 묻는다.** 혼자 붙잡는 시간이 이 계획의 가장 큰 적이다. 단, ③ 단계에서는 원본을 "확인하고 닫기"로만 쓴다.
4. **요일과 시간을 고정한다.** 계획의 완성도보다 매주 커밋이 있는 상태가 중요하다. 3주차쯤 열정이 식었을 때 이 규칙이 버티게 한다.

---

## 5. 두 저장소의 역할

| | `pricewatch` | `go-study` |
|---|---|---|
| 무엇 | 제품. 배포되고, 남이 쓰고, 이력서에 링크할 것 | 연습장. 실험, 예제, 막혔던 것 |
| 누구 코드 | **내 코드만.** 완성본은 출발점일 뿐, 10주차 이후엔 전부 내가 다시 쓴 것 | Claude 의 예제와 내 실험이 섞여도 된다 |
| 구조 | 제품 구조 (`cmd/`, `internal/`) | 폴더 하나 = 주제 하나. 각각 독립 모듈 |
| 기록 | README, ADR, 커밋 메시지 | 각 폴더 README 에 세 줄: **해보려던 것 / 막힌 것 / 알게 된 것** |

"이거 헷갈려" 싶은 주제가 생기면 → `go-study` 에 폴더 하나.
pricewatch 에 넣을까 말까 고민되는 실험 → 무조건 `go-study`.

---

## 6. 자료는 이것만

| 자료 | 용도 |
|---|---|
| `go-study/tour/` | 문법 전체를 실행되는 파일 5개로 넓게. 처음 한 번 |
| `go-study/basics/` | pricewatch 에 나오는 것만 깊게. 처음 한 번 |
| [Go by Example](https://gobyexample.com) | 사전. 기억 안 날 때 그 항목만 |
| [Effective Go](https://go.dev/doc/effective_go) · [Code Review Comments](https://go.dev/wiki/CodeReviewComments) | 1주차에 한 번, 10주차 리팩토링 때 체크리스트로 다시 |
| *100 Go Mistakes and How to Avoid Them* | 2주차부터 주당 10개, 10주차에 완독 |
| [pkg.go.dev](https://pkg.go.dev) | 표준 라이브러리 문서. 블로그보다 낫다 |
| Claude | "이 코드는 왜 이렇게 짰어?", "더 Go 다운 방법 있어?", 코드 리뷰 |

이 밖의 자료를 추가하고 싶어지면, 먼저 위의 것을 다 썼는지 묻는다.

---

## 7. Claude 와 일하는 방법

- **기본은 내가 치고, Claude 는 옆에 있는 시니어.** 설계 방향, 코드 리뷰, "왜?", 에러 원인 분석을 맡긴다.
- Claude 가 코드를 통째로 써주는 경우는 셋뿐: (1) 내가 명시적으로 "이건 그냥 짜줘"라고 했을 때, (2) 학습 목표와 무관한 보일러플레이트(Dockerfile, CI, compose), (3) 30분 넘게 막혀서 예시가 필요할 때.
- Claude 가 써준 코드는 복사하지 말고 한 줄씩 "왜?"를 물어도 된다.
- 새 대화를 시작할 때는 맥락 한 줄을 붙인다: "pricewatch 5주차, 스케줄러 다시 짜는 중, 저장소 github.com/CodingToastBread/pricewatch". 이전 대화는 기억하지 못한다.
- 다른 언어 비유는 요청하지 않는 한 쓰지 않는다.

---

## 8. 진행 순서 (2026-09)

1. `tour/` 다섯 개 순서대로 (2h) → 3절 방식으로 고른 블록만 손으로
2. `basics/` (1h, tour 와 겹치는 부분은 빠르게)
3. `fetchall/` README 의 1~6단계, 단계마다 커밋 (몸풀기. 3단계까지만 되어도 목적 달성)
4. 9/14 부터 pricewatch 1주차: 실행 → 요청 하나 끝까지 추적 → 패키지 의존 그림. **코드는 쓰지 않는다.**
5. 이후는 로드맵 페이지대로. 체크박스와 주별 기록을 남긴다.

---

## 9. 흔들릴 때

- "이해가 안 돼서 진도가 안 나가" → 이해 안 되는 줄을 질문으로 바꿔 Claude 에게. 30분 규칙.
- "읽기만 하고 있는 것 같아" → 지금 읽는 패키지의 폴더를 지우고 테스트만 남겨라.
- "다시 짜는 게 안 돼" → 원본을 열어 확인하고 닫아라. 다시 지워라. 세 번째부터 된다.
- "이번 주 시간이 없어" → 커밋 하나. 기능이 반쪽이어도 된다. 주를 비우지 않는 게 전부다.
- "다른 자료를 찾아보고 싶어" → 6절. 위의 것을 다 썼는가?
